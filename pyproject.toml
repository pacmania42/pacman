[project]
name = "pacman"
version = "0.1.5"
description = "The classic pacman game implemented in python & arcade"
readme = "README.md"
requires-python = ">=3.10"

dependencies = [
    "arcade>=3.3.3",
    "mazegenerator",
    "pydantic>=2.13.4",
]

[tool.uv.sources]
mazegenerator= { path = "wheels/mazegenerator-2.1.0-py3-none-any.whl" }

[dependency-groups]
dev = [
    "flake8>=7.3.0",
    "mypy>=2.3.0",
    "pytest>=9.1.1",
]

[tool.mypy]
plugins = ["pydantic.mypy"]
mypy_path = "stubs"

[tool.ruff]
line-length = 79
[tool.ruff.lint]
select = ["E", "F", "B", "I"]

[tool.pytest.ini_options]
pythonpath = ["."]
